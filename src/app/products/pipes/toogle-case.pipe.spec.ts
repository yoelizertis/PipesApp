import { ToogleCasePipe } from './toogle-case.pipe';

describe('ToogleCasePipe', () => {
  it('create an instance', () => {
    const pipe = new ToogleCasePipe();
    expect(pipe).toBeTruthy();
  });
});
